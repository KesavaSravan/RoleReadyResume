const fs = require('fs');
const pdfParse = require('pdf-parse');

async function testPdf() {
    // try to load some dummy pdf if exists, or how can I test it?
    console.log("pdfParse version: ", require('pdf-parse/package.json').version);
}
testPdf();
